# Better Auth base URL (your app URL)
BETTER_AUTH_URL="http://localhost:3000"

# Better Auth secret (replace with a strong random value in real environments)
BETTER_AUTH_SECRET="replace-with-a-long-random-secret"
